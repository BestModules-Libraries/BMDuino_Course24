/*
程式流程解說：

setup()

初始化模組與序列埠。

模組需要大約 3 分鐘的預熱時間，
否則感測值會不準確。

loop()

持續檢查模組是否有新資訊輸入。

若有，將資料封包讀入 moduleInfo[]，
並呼叫 printInfo() 輸出數據。

printInfo()

輸出三個重要資訊：
✅ 氣體警報門檻值（當氣體濃度超過此數值時觸發警報）。
✅ 目前氣體濃度（單位：PPM）。
✅ 模組的 AD 原始值（供校正或偵錯使用）。
*/