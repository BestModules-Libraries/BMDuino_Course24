/*
這份程式主要是用 BMC36M0x1 無線模組 來實現「配對、傳輸、接收」流程。
總結流程

按 KEY1 (D8) → 進入配對模式 (MRX 燈閃爍，持續 8 秒)。

按 KEY3 (D10) → 發送配對請求，若成功 → "Pair Success"。

配對後進入接收模式：

按 KEY3 (D10) → 發送一筆資料 (TXDATA[])。

另一塊板子接收到資料 → MRX 燈閃爍，序列埠顯示資料內容。

按 KEY2 (D9) → 在接收模式與休眠模式之間切換。

*/