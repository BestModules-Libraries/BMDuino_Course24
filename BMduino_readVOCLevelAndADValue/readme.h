/*
補充說明

VOC 等級（VOC Level）

這個值是模組內部根據感測到的氣體濃度計算出的等級
（可能是 0~255 或特定區間）。

VOC A/D 值（VOC A/D Value）

這是感測器將氣體訊號經過類比數位轉換（ADC）後的原始值，
數值越高表示濃度越高。

預熱的重要性

VOC 感測器啟動後需要一定的時間穩定化，
如果不預熱就讀值，
會有很大誤差。

自動模式 vs 命令模式

AUTO_MODE：模組會自己每秒傳一次資料。

CMD_MODE：必須用指令去詢問模組才會回傳資料。

*/